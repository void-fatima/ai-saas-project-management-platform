# Authentication recovery and audit revalidation

Baseline: `cb86a68`, recovered from pushed auth UI `3f80619` and session hardening `321d3c9`. These classifications are source inspection, not runtime verification. They precede new implementation.

| Finding | Before work | Current code evidence                                                                                                                                                                                                                   | Final status                               | Change made                                                                                                |
| ------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| F01     | RESOLVED    | `session-auth.guard.ts` only maps `InvalidSessionError` to 401; operational errors become safe 500; neither clears cookies. HTTP lifecycle regressions cover lookup, rotation, and recheck failures.                                    | RESOLVED (source)                          | Recovered historical fix.                                                                                  |
| F02     | RESOLVED    | `prisma-auth.repository.ts` uses conditional `updateMany`, predecessor hash/grace, and service revalidation after a lost race. PostgreSQL and HTTP regressions exist.                                                                   | RESOLVED (source; DB verification pending) | Recovered historical fix.                                                                                  |
| F03     | OPEN        | `createSession` uses an ordinary transaction, no per-user lock, and orders only by `createdAt` before skipping ten.                                                                                                                     | OPEN                                       | None yet.                                                                                                  |
| F04     | PARTIAL     | Recovered baseline: `AuthView` submitted forms but `App` only toggled a view. Current `use-session.ts` bootstraps through `/auth/me`, protects rendering, bounds requests, rejects obsolete responses, and supports both logout scopes. | PARTIAL                                    | Implemented frontend session lifecycle; component verification passes, real browser/DB validation pending. |
| F05     | PARTIAL     | `session-lifecycle.integration.spec.ts` covers real conditional rotation, expiry, and revocation. Registration rollback, concurrent cap, verification/reset are absent.                                                                 | PARTIAL                                    | Recovered PostgreSQL suite.                                                                                |
| F06     | OPEN        | `turbo.json` has no env declarations or root env inputs; Vite, Nest, and Prisma load root env.                                                                                                                                          | OPEN                                       | None yet.                                                                                                  |
| F07     | OPEN        | `environment.validation.ts` validates fields independently; `WEB_ORIGIN` accepts arbitrary URLs.                                                                                                                                        | OPEN                                       | None yet.                                                                                                  |
| F08     | OPEN        | Rotation extends expiry; no absolute limit or cleanup; `lastSeenAt` changes only on creation/rotation.                                                                                                                                  | OPEN                                       | None yet.                                                                                                  |
| F09     | PARTIAL     | Global in-memory throttler exists; `main.ts` does not trust forwarding headers. Proxy/replica topology undocumented.                                                                                                                    | PARTIAL                                    | None yet.                                                                                                  |
| F10     | PARTIAL     | Cookies and safe guard errors exist; route header decorators do not establish an early policy for guard failures. No origin enforcement/security-header setup. Bootstrap logs raw exception messages.                                   | PARTIAL                                    | Recovered cookie/error hardening.                                                                          |
| F11     | OPEN        | `health.controller.ts` returns constant `{ status: 'ok' }`; startup DB check is not ongoing readiness. Global throttler also applies to health.                                                                                         | OPEN                                       | None yet.                                                                                                  |
| F12     | OPEN        | `App.checkApi` accepts any successful status, lacks timeout and request ordering; retries have no abort controller.                                                                                                                     | OPEN                                       | None yet.                                                                                                  |
| F13     | OPEN        | Dialogs use `aria-modal` but lack containment/inertness/restoration. Search input has no active-option relationship; trigger opens on restored focus.                                                                                   | OPEN                                       | None yet.                                                                                                  |
| F14     | OPEN        | `Sidebar` buttons rely on child text hidden with `display: none` below 1180px; no explicit labels.                                                                                                                                      | OPEN                                       | None yet.                                                                                                  |
| F15     | OPEN        | `App` hardcodes quality gates as Verified; ecosystem labels unimplemented AI Operational. Activity feed is marked demo but uses live/verified wording.                                                                                  | OPEN                                       | None yet.                                                                                                  |
| F16     | OPEN        | Compose publishes `5432:5432` on all host interfaces.                                                                                                                                                                                   | OPEN                                       | None yet.                                                                                                  |
| F17     | OPEN        | Four imported PNGs total 5,135,491 bytes: orb, orbits, branches, halo. Orbital disc is unused.                                                                                                                                          | OPEN                                       | Visual replacement requires browser validation.                                                            |
| F18     | OPEN        | Dialog CSS uses `overflow: hidden` without viewport max height; several dialog labels are under 0.75rem. Auth fields already use 1rem.                                                                                                  | OPEN                                       | None yet.                                                                                                  |
| F19     | OPEN        | API applies trim after `z.email`; web validates raw whitespace.                                                                                                                                                                         | OPEN                                       | None yet.                                                                                                  |
| F20     | OPEN        | Environment selector is an enabled button without action; overview navigation has no action; stale numbered-issue copy remains in shell.                                                                                                | OPEN                                       | None yet.                                                                                                  |

No later product capability is authorized by this audit. The product roadmap remains authoritative for deferred scope.

## Verification checkpoint — 2026-09-13

Recovery completed on `feature/authentication`: `e132010` (auth UI), `cb86a68` (session hardening). Only README/roadmap conflicts occurred; both capabilities and outstanding scope were retained. Historical author attribution and logical commits were preserved.

- Frozen dependency installation with pinned pnpm 10.15.0 succeeded, including Prisma 7.9.1 generation. pnpm reported ignored dependency build scripts; the actual API tests and both builds nevertheless succeeded.
- Recovered baseline: web 34/34 tests, API 24/24 tests; web/API lint, typecheck, and builds passed.
- Current frontend slice: web 43/43 tests across four files passed; web lint/typecheck passed; production web build passed. The two additional final tests cover bootstrap timeout and tab-return revalidation.
- Root formatting initially failed on the Windows CRLF checkout. `.gitattributes` now specifies LF for text (CRLF for batch scripts); canonical formatting and root `format:check` pass. These line-ending changes do not change backend behavior.
- `git diff --check` passes.
- `pnpm db:up`: **BLOCKED BY ENVIRONMENT**, Docker executable unavailable. No migrations applied, PostgreSQL integration tests run, or database data touched in this environment.
- Real browser E2E: **BLOCKED BY ENVIRONMENT** for the required real backend/PostgreSQL flow. Component fetch stubs are not E2E evidence. No browser framework or CI gate has been added yet.
- Push of the recovered commits was rejected by GitHub GH007 (private committer email). No branch was published. Correction of unpublished recovery commits requires explicit approval because history rewriting was prohibited. New frontend work remains uncommitted at this checkpoint pending that decision.

The next slices remain verification/recovery delivery and token persistence, concurrency-safe session limits and lifetime/retention, configuration/security, health/readiness, accessibility, real PostgreSQL and browser coverage, and CI. Later product capabilities remain deferred in the roadmap.

### Readiness checkpoint — 2026-09-18

Liveness and bounded database readiness are separate. The frontend validates readiness responses, times out, and ignores stale probes. API lint, typecheck, 45 unit/HTTP tests, and build passed; web lint, typecheck, 54 component/unit tests, and build passed. Real PostgreSQL and browser checks remain unverified: Docker reports that the Desktop Linux engine pipe is absent. These mocked health regressions do not establish database connectivity.

### Continued hardening at b1518c0

Revalidated: verification/reset, lifetime, environment/Turbo, readiness, UI fixes OPEN; session cap, HTTP policy, DB coverage PARTIAL; PostgreSQL and real E2E execution BLOCKED BY ENVIRONMENT at initial inspection. On September 17 Docker Desktop was found installed and started, but engine availability is still being checked. Two backup stashes retained. Session creation and logout-all now share a user-row lock and deterministic pruning; real concurrency regression added, execution blocked.
